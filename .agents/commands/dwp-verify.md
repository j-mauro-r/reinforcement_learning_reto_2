---
description: Verify repository and plan conformance against the installed DWP specification
---

# #dwp-verify

Thin repository alias. Invoke the installed root **deepworkplan** skill, route the request to its **verify** sub-skill, and follow that procedure as authoritative.

Before execution, read `AGENTS.md` plus the relevant repository sources (`enunciado-reto.md`, `lineamientos-transversales.md`, and model-local docs when present). Keep all DWP operational output under the gitignored `.dwp/` workspace.
