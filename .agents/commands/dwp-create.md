---
description: Create a Lite-first Deep Work Plan for repository work; promote to Full only when needed
---

# #dwp-create

Thin repository alias. Invoke the installed **deepworkplan-create** skill and follow it as the authoritative procedure.

Before execution, read `AGENTS.md` plus the relevant repository sources (`enunciado-reto.md`, `lineamientos-transversales.md`, and model-local docs when present). Keep all DWP operational output under the gitignored `.dwp/` workspace.
