# GitHub Copilot repository instructions

`AGENTS.md` is the single source of truth for this repository. Read and follow it before proposing or applying changes.

For DWP operations, use the repository command procedures under `.agents/commands/` and the installed `deepworkplan` skill. Do not duplicate DWP lifecycle rules here.
