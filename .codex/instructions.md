# Codex repository instructions

Read `../AGENTS.md` and treat it as the single source of truth for repository conventions, DWP workflow, validation gates and delivery rules.

Use the installed `deepworkplan` skill for DWP lifecycle operations; repository aliases live in `../.agents/commands/`.
