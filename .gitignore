# Deep Work Plan operational workspace (DWP v4)
.dwp/
tmp/
scratch/

# Python / notebooks
__pycache__/
*.py[cod]
.pytest_cache/
.venv/
venv/
.ipynb_checkpoints/

# Local environment / OS
.env
.env.*
!.env.example
.DS_Store

# Adroit candidate runs, checkpoints and replay buffers (not canonical delivery)
/1-AdroitHandDoor/runs/

# Fetch candidate runs, checkpoints and replay buffers (not canonical delivery)
/2-FetchReachDense/runs/

# Hand candidate runs and checkpoints (not canonical delivery)
/3-HandReachDense/runs/

# Archived unnormalized PPO runs
/3-HandReachDense/results_archived_unnormalized_ppo/
